# OpenAI API key — required for Phase 2 classification
OPENAI_API_KEY=sk-your-key-here

# Model to use for email classification
MODEL_NAME=gpt-4o-mini

# Number of unread emails to fetch and classify per run
MAX_EMAILS=10
